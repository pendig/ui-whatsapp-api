import ComponentsAppsTemplate from '@/components/template/components-apps-template';
import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Template',
};

const Template = () => {
  return <ComponentsAppsTemplate />;
};

export default Template;
