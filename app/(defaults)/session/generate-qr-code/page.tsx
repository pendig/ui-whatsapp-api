import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Generate QR Code',
};

const GenerateQrCodePage = () => {
    return <div>Generate QR Code Page</div>;
};

export default GenerateQrCodePage;
